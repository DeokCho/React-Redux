/*let color = {}
let initialState = color? {color}: {}

export function colorReducers(state, action){
    switch(action.type){
        default:
            return state
    }
}*/

/*
let color = {}
let initialState = color? {color}: {}

export function colorReducer(state, action){
    switch(action.type){
        default:
            return state
    }
}*/

let color = {}
let initialState = color? {color}: {}

export function colorReducer(state=initialState, action){
    switch(action.type){
        default:
            return state
    }
}